# Runtime models

Validated Core ML packages used by the Xcode app target belong here. The GPA conversion tool writes
experimental exports to `Models/Generated`; copy the selected package here and add it to Copy Bundle
Resources when it is ready to ship.
